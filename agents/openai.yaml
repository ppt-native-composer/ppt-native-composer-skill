interface:
  display_name: "PPT Native Composer"
  short_description: "Reconstruct approved layouts as editable PPTX"
  default_prompt: "Use $ppt-native-composer to reconstruct my approved artwork and copy as a hybrid editable PPTX and verify the result."
